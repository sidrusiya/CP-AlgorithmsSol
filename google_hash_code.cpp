#include<bits/stdc++.h>
using namespace std;

signed main() {
    int pizzano,no2mem,no3mem,no4mem;
    cin>>pizzano>>no2mem>>no3mem>>no4mem;
    vector<vector<string>> ingr(pizzano);
    for(int i=0;i<pizzano;i++) {
        int x;
        cin>>x;
        for(int j=0;j<x;j++) {
            string s;
            cin>>s;
            v[i].push_back(s);
        }
    }

}
