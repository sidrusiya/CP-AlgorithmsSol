#include<bits/stdc++.h>
using namespace std;

signed main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        vector<pair<int,int>> v(n-1);
        for(int i=0;i<(n-1);i++)
        {
            cin>>v[i].first>>v[i].second;
        }
        int a[n];

    }
}
